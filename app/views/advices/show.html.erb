<div class="max-w-3xl mx-auto bg-white shadow-md rounded-lg p-6 md:p-8 mt-6 leading-relaxed relative">

  <div class="flex flex-col md:flex-row items-start space-y-4 md:space-y-0 md:space-x-6 mb-8">
    <div class="bg-base-100 rounded-lg p-4 shadow-inner flex-1 relative">
      <% if @advice.user.profile_image.present? %>
        <%= image_tag(@advice.user.profile_image.url, 
                      alt: "#{@advice.user.username}さんのプロフィール", 
                      class: "w-12 h-12 rounded-full object-cover shadow-sm border absolute -top-4 -left-4") %>
      <% else %>
        <%= image_tag("default_profile_image.png", 
                      alt: "#{@advice.user.username}さんのプロフィール", 
                      class: "w-12 h-12 rounded-full object-cover shadow-sm border absolute -top-4 -left-4") %>
      <% end %>
      
      <h2 class="text-xl font-semibold text-gray-800 mb-2 ml-16">
        <%= @advice.user.username %>さんの相談
      </h2>
      <p class="text-gray-700 ml-16"><%= @advice.input %></p>
    </div>
  </div>

  <!-- AIコーチのアドバイス -->
  <div class="flex flex-col md:flex-row items-start space-y-4 md:space-y-0 md:space-x-6 mb-8">
    <div class="bg-blue-50 rounded-lg p-4 shadow-inner flex-1 relative">
      <%= image_tag 'coach.svg', 
                    alt: "AIコーチのプロフィール", 
                    class: "w-12 h-12 rounded-full object-cover shadow-sm border absolute -top-4 -left-4" %>
      
      <h2 class="text-xl font-semibold text-gray-800 mb-2 ml-16">AIコーチのアドバイス</h2>
      <p class="text-gray-700 ml-16"><%= @advice.response %></p>
    </div>
  </div>

  <div class="bg-base-100 rounded-lg p-4 mb-8 text-center">
    <p class="text-gray-600">
      他にも聞いてみたいことがあれば、いつでも相談してくださいね！<br>
    </p>
  </div>

  <div class="flex flex-col md:flex-row justify-center space-y-4 md:space-y-0 md:space-x-6">
    <%= link_to "マイページに戻る", mypage_path, class: "btn btn-secondary text-base-100 w-full md:w-auto shadow-md hover:shadow-lg transition-all" %>
    <%= link_to "他のアドバイスを受ける", new_advice_path, class: "btn btn-primary text-base-100 w-full md:w-auto shadow-md hover:shadow-lg transition-all" %>
  </div>
</div>
