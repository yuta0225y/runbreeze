<div class="navbar bg-base-100 shadow-lg fixed bottom-0 left-0 right-0 z-10">
  
  <!-- ホーム -->
  <%= link_to posts_path, class: "flex flex-col items-center flex-1 py-2 text-gray-500 hover:text-gray-700 transition-colors duration-200" do %>
    <%= image_tag 'home.svg', alt: "Home", class: "h-6 w-6 mb-1" %> 
    <span class="text-xs text-gray-500">ホーム</span>
  <% end %>

  <!-- ポスト -->
  <%= link_to new_post_path, class: "flex flex-col items-center flex-1 py-2 text-gray-500 hover:text-gray-700 transition-colors duration-200" do %>
    <%= image_tag 'post.svg', alt: "Create", class: "h-6 w-6 mb-1" %> 
    <span class="text-xs text-gray-500">投稿</span>
  <% end %>

  <!-- マイページ -->
  <%= link_to mypage_path, class: "flex flex-col items-center flex-1 py-2 text-gray-500 hover:text-gray-700 transition-colors duration-200" do %>
    <%= image_tag 'mypage.svg', alt: "My Page", class: "h-6 w-6 mb-1" %> 
    <span class="text-xs text-gray-500">マイページ</span>
  <% end %>
</div>
